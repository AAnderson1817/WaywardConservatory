# Project instructions

- Work from a normal local checkout outside OneDrive. The user explicitly prohibits using OneDrive for this project or future project storage.
- Use Git and the GitHub repository `AAnderson1817/WaywardConservatory` for source control. Keep completed changes committed and push authorized work; report any push/authentication failure instead of implying a remote backup exists.
- Do not include dependencies, generated builds, local credentials, raw task histories, or transient screenshots in source control.
- Preserve the compact arcade menu: only the arcade title, five game titles, and their one-sentence descriptions.
- Adjacent is approved. Ballast has twelve levels and remains awaiting review. Do not implement another game without the user's explicit approval.
- Run the model tests and production build for gameplay changes. Browser tests use installed Edge/Chromium and write temporary evidence to `artifacts/`.
